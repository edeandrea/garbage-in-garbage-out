# Chat

Live-demo Quarkus web application serving the RAG chatbot UI. Supports switching between RAG modes (A/B/C) at runtime through the UI, displaying both the generated answer and the retrieved chunks used to produce it.

## Run

```shell
./mvnw -f chat quarkus:dev
```
